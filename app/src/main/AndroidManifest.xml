<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kamennuzkypapir">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KamenNuzkyPapir">

        <!-- Start screen allowing user to choose between single and two player modes. -->
        <activity
            android:name=".StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Single‑player game against the phone -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <!-- Two‑player game on one device -->
        <activity
            android:name=".TwoPlayerActivity"
            android:exported="true" />

        <!-- About page providing author, version and release date info -->
        <activity
            android:name=".AboutActivity"
            android:exported="true" />

    </application>

</manifest>
